import { SectionWrapper } from "@/app/layouts/SectionWrapper";
import { SectionNotFound } from "@/widgets/sections/SectionNotFound";

export function PageNotFound() {
  return (
    <SectionWrapper>
      <SectionNotFound />
    </SectionWrapper>
  );
}
