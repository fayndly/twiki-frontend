import { SectionWrapper } from "@/app/layouts/SectionWrapper";
import { SectionSettings } from "@/widgets/sections/SectionSettings";

export function PageSettings() {
  return (
    <SectionWrapper>
      <SectionSettings />
    </SectionWrapper>
  );
}
