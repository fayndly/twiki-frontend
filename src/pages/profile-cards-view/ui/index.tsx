import { ListProfileCards } from "./ListProfileCards";

import { SectionWrapper } from "@/app/layouts/SectionWrapper";

export function PageProfileCardsView() {
  return (
    <SectionWrapper>
      <ListProfileCards />
    </SectionWrapper>
  );
}
